# 隐私与数据上报说明

## 当前交付：每日挑战 / 同房续局隐私增量（2026-09-23，已确认）

- 正文唯一来源仍为 `match3-wechat/js/core/privacy-policy.js`，完整确认稿见 `privacy-policy-review.md`。版本 `2026-09-23.1`，保留原生效日9月19日，更新日9月23日，11节；运营方本轮明确确认采用，`approvedForRelease=true`。正文内容未再改动，相关语法、privacy-reader（已确认副标题及正文一致性）、release-gates与diff检查通过；不代表后台设置或真机验收完成。
- 新增每日挑战可信OpenID与用户哈希、日期/版本/局标识、操作检查点、成绩、奖励凭证和本地记录；哈希不是匿名数据。对局有效7天与云记录超过30天定时清理分别说明；对局按createdAt、每日进度按updatedAt清理，限流清理计时在成功新开局时更新，日志/备份另按服务配置。
- 补充PvP局次、累计胜场、双方续局确认和结算凭证；续局不重置房间创建时间及7天清理基准。明确普通单人进度/完整钱包不云同步，删除本地数据不删除云端记录、不重置每日次数。
- 2026-09-22用户已授权恢复官方优先入口：设置→隐私说明先调用 `wx.openPrivacyContract`，缺失/失败/超时才回退完整本地长文；重复点击合并，迟到回调不得打断新页面。阅读或返回不代表同意，也不替代必要授权；本轮不修改此行为及平台隐私弹窗模式。
- 用户已反馈后台指引可以修改且已更新；仍需与本次新增处理逐项保持一致，不能继续套用“不处理用户信息”。本轮没有读取或修改后台、调用真实微信接口，不将本地回退成功当官方协议已可读。
- 定向验证：audio、obstacle、open-source-and-assets、privacy-reader、privacy-entry通过；阅读器覆盖11节全文、确认稿逐段一致、320/390/430布局、滚动/末尾/返回与确认门禁。样式/首尾/触控逻辑不变，复用既有独立视觉证据，不复用旧正文的滚动高度；没有本轮真机验收。
- 独立内容审阅指出跨日恢复需本地局标识，已据实补正文/确认稿并新增断言：清除本地数据可能导致有效期内旧日局无法恢复。正文/测试语法、privacy-reader及包体重新通过；其他未变判据复用。
- 正文确认标记false→true少1字节，确认时静态主包4,178,174字节；之后经用户批准的无损引用清理已降为3,732,736 / 4,194,304字节，最新预算/素材边界见 `package-structure.md`。不是微信最终打包值。云端未改，既有每日部署证据见 `daily-activation.md`。未提交、推送、上传、提审或发布。

## 历史交付：本地隐私协议页（2026-09-19，入口与确认状态已被上文取代）

- 用户选择完整本地协议阅读入口，后续可在官方接口恢复且再次授权后切回；当前没有切回、推送、上传或提审授权。
- 公开信息已由用户提供：运营主体“任务”，隐私邮箱 `452330817@qq.com`，生效日期2026年9月19日。不将此公开信息写入共享记忆。
- 正文唯一来源：`match3-wechat/js/core/privacy-policy.js`；10节包含本地记录、PvP、可选头像、运行环境、广告、共享、安全/删除、权利、未成年人及更新。
- 页面入口：设置 → 隐私说明 → 本地长文 → 返回设置；支持触摸滚动、上下翻屏、末尾提示，重新进入从顶部开始；不调用官方协议API、云函数或存储，不设同意墙。不等于整个游戏不联网或已完成必要授权。
- 状态：用户于2026年9月19日明确确认已展示的完整正文，`approvedForRelease=true`，页面副标题改为“猫猫开心消·可离线阅读”。该标记只代表本地正文确认，不代表后台合规事项完成或授权提交、上传、发布；正文内容本轮未改动。
- 平台未解项：后台“不处理信息”声明与PvP等实际处理不符，去完善后选项灰色；用户正在处理。开发者工具曾返回 `openPrivacyContract:fail invalid protocol invalid url: undefined`，该症状不证明所有微信环境的根因。独立审阅还指出云日志/备份保存地点与期限未有配置证据，必要授权和未成年人保护要求需结合实际平台设置复核；本地展示不能替代这些事项。
- 新增模块为原生JavaScript及Canvas文字，不引入图片、字体、依赖或远程网页。

### 本轮验证与待确认

- 相关脚本 `node --check` 与 `git diff --check` 通过。
- `node match3-wechat/test/privacy-reader.js`：真实Main触摸回调、离线入口、10节全文换行不丢字、排版缓存、3尺寸安全区、上下翻屏/拖动夹紧、末尾可达、返回及重新打开、退后台清理、正文确认门禁单测通过。
- 受影响的 `ui-smoke.js`、保留官方接口的 `runtime.js` 测试通过；后续仅正文及阅读器定向修改复用不受影响证据，未重复全游戏套件。
- `package-budget.js` 正文确认前源码静态估算3,859,625 / 4,194,304字节，59文件；相对原基线新增16,869字节。确认操作仅将运行时代码中的 `false` 改为 `true`，少1字节，复用已有包体预算证据；不是微信上传后的计量结果。
- `release-gates.js` 确认前按预期因正文未确认失败；用户确认后复测通过。同步复测 `privacy-reader.js`（含已确认副标题与无草案提示的断言）、预览脚本语法及差异检查通过。正文/布局/交互未改，复用之前独立代码和画面审阅；该代码级检查不验证后台声明、平台设置和真机，不能视为上线全项放行。
- 独立代码/内容审阅：原审阅者确认无阅读器/入口阻断缺陷；其指出的“OpenID传输必要用途”无据表述已改为仅描述实际响应，定向复查通过。正文确认稿10节与代码 `sections()` 标题、顺序和正文一致。
- 视觉证据：沿用实际Main/UI的本地隔离Canvas预览（内存存档、无微信接口与云服务）；新视觉审阅者使用固定Luna max worker，替代工具中不存在的专用finish-reviewer。独立判定 `ship` 仅限画面与触控：320×568、390×844、430×932均验证首屏、滚动到末尾与返回设置，无画面阻断项；窄屏邮箱自然换行，字符完整。Canvas不运行仅识别HTML/CSS的机械detector。
- 画面证据对应最终一处PvP中段文案缩短前的本地预览快照，阅读器/样式及首尾文本未变；独立审阅确认可复用这些不受影响的视觉/触控判据。最新正文的完整排版/滚动夹紧由定向单测覆盖，不复用旧 `maxScroll` 数值。最终源预览已另行启动供用户核对正文。
- 微信开发者工具及手机上的新本地页面尚未验收，后台配置未变，未提交Git、推送GitHub、上传或提审。公开主体、邮箱可用性、云服务部署/日志/备份期限及必要授权仍需运营方按实际配置复核。

### Direction contract

Read模式，延续已批准月光花园世界：深靛蓝正文、珍珠浅色阅读面板、现有蓝色圆角按钮和统一字体。设置页布局保持原样，只扩展隐私入口；不重新选择视觉方向。
首屏为小标题面板、主体/邮箱/日期和协议正文；长文区域独立裁切并显示位置条，底部44px上下屏控件及48px返回设置始终可用。正文约17.6逻辑像素，行高28；按真实宽度换行、缓存排版。无新素材、动效或付费生成。
完成条件：320×568、390×844、430×932可读与可操作，正文无丢字、末尾可达、离线入口与返回验证；独立审阅代码/内容及实际画面。视觉页面验收不等于正式隐私内容或微信审核放行。

## 发布前必须由运营方确认

以下内容不能由代码或开发者代为编造，必须由运营主体在微信公众平台完成并保持一致：

- 运营主体名称：任务（用户提供，需与后台主体一致）
- 联系方式：452330817@qq.com（用户提供的公开隐私邮箱）
- 生效日期：2026年9月19日（用户提供）
- 本地协议完整正文：2026-09-23.1增量稿已于2026年9月23日确认
- 微信小游戏平台隐私保护指引/隐私合同内容：用户反馈已更新，本次仍需补充/核对每日挑战与同房续局披露，并验证官方页面实际可打开

发布前不得保留“待用户填写”占位信息。

## 当前代码边界

- `REPORTING_CONFIG.enabled` 默认是 `false`，`eventIds` 与 `monitorNames` 默认为空；未显式配置映射时不调用 `wx.reportEvent` 或 `wx.reportMonitor`，不上传数据。
- 本地去除直接身份字段的事件仍按现有策略保存在本地，最多 200 条、保留 7 天；全局错误只记录类别和计数，不保存原始 message、reason、stack、用户标识或设备信息。
- 核心漏斗覆盖首页曝光、单人选关/开局/完成，以及 PvP 点击/建房/邀请/加入/准备/开局/完成/异常；属性只接受白名单中的关卡号、结果枚举、星级、时长档位、复活次数和错误类别等低敏标量。事件不得包含房间号、OpenID、昵称、头像或原始错误文本。
- PvP 云数据库为完成匹配、同步和结算保存双方 OpenID、固定展示名、比分、道具和房间状态。等待邀请10分钟后业务失效；创建超过7天的房间由 `cleanup-battle-rooms` 每日清理，并非精确7天即刻删除。当前道具效果响应包含 `fromOpenid/toOpenid`，不得宣称OpenID绝不返回客户端；不在此任务中无授权修改对战协议。
- PvP 建房限流在服务端保存按OpenID生成的SHA-256截断摘要计数文档，不保存明文OpenID，也不向客户端返回摘要或内部计数。它仍是可关联用户的标识，不是完全匿名数据。计数文档与房间共用仅服务端访问的 `battle_rooms` 集合，清理计时字段在成功建房后更新。
- PvP同房续局记录局次、累计胜场、续局确认和结算凭证；每日挑战在仅服务端访问的 `daily_runs` / `daily_progress` 保存用户哈希、版本/日期、最多25步交换坐标检查点、成绩和奖励收据，按上文期限清理。客户端每日记录和钱包凭证不是完整云同步，也没有云端30天自动清理规则。
- PvP 等待页可由用户主动点击头像，选择使用微信头像；拒绝或接口不可用时继续使用项目内猫咪头像，不影响建房、加入、准备或开局。头像 URL 只保存在当前设备本地，不上传 `battle_rooms`、云函数或匿名埋点。上线前需在平台隐私保护指引中如实声明该可选本地用途并完成真机授权/拒绝回归。
- 客户端云开发初始化不开启可选 `traceUser` 访问追踪；对战身份只由云函数侧的可信 OpenID 校验。
- 设置页“隐私说明”优先调用 `runtime.openPrivacyContract`，不可用时回退完整本地页；官方可读性和后台内容一致性仍需平台/真机确认。
- 云函数异常分支会把操作类型与原始异常写入云日志；云服务日志/备份留存不适用客户端事件或房间记录的7天清理约定。
- `wx.getUpdateManager`、`wx.onError`、`wx.onUnhandledRejection` 及隐私接口均按平台能力检测使用。

上线前需由运营方根据实际数据处理、广告和云开发情况填写并复核平台隐私合同；代码中的默认关闭不替代平台配置和合规审核。产品负责人已确认 CloudBase 定时触发器生效，并用测试数据核对了 7 天清理结果。

# 双账号 PvP 真机验收清单

## 2026-09-22 右上角好友分享入口修复（本地完成，待真机）

- 用户确认故障为“没有分享入口”并授权修复。`js/main.js`启动时原来仅注册`wx.onShareAppMessage`文案，缺少菜单启用；现于注册后调用`wx.showShareMenu({ menus: ['shareAppMessage'] })`，保留API存在性检查。普通分享文案、对战邀请参数与云端逻辑不变。
- 官方依据：本次读取微信小游戏[showShareMenu](https://developers.weixin.qq.com/minigame/dev/api/share/wx.showShareMenu.html)与[onShareAppMessage](https://developers.weixin.qq.com/minigame/dev/api/share/wx.onShareAppMessage.html)文档，前者控制详情界面的分享按钮可用性，后者仅监听转发并提供内容。
- 验证：当前工作区`main.js`语法、`test/battle-client.js`及聚焦差异空白检查通过；隔离Node VM执行真实Main构造器，修改前菜单调用为0，修改后启用好友菜单1次，均到达主循环；分享标题、邀请query保持正确，缺少API时仍可启动。使用假wx，不接触账号、网络或真实存档；不能代替微信真机菜单显示与卡片打开验证。
- 下一步：将此修复纳入后续获准上传的客户端候选，在手机确认右上角“发送给朋友”出现、卡片可发送且好友可打开。当前存在其他未提交功能变更，不能将整个工作区自动视为已验收发布包；本次未上传、提审、发布、提交或推送，不将此修复视为历史云端建房故障已解决。结论已记入本权威记录，无需重复写入共享记忆候选。

## 2026-09-22 等待房间修复（本地完成，待真机）

- 授权：用户“推进修复”；仅本地修复头像异常卡帧与等待页同步无界等待，不改隐私授权模式、云权限或对战规则，不上传/部署/提交/推送。保留已有并行功能变更。不能据此宣布历史正式版建房间歇失败已解决。
- `platform/user-profile.js`：创建/注册/销毁可选头像按钮、图片与本地存储API异常被隔离；按钮异常后本次运行禁重试，默认猫咪继续展示。失效按钮回调不写头像，图片请求有代次保护。只记录固定类别的去重事件，不保留异常对象、头像地址或身份数据；更换不可用时隐藏点击提示。
- `net/cloud-battle.js` / `main.js`：为query、configureItems、ready、leave设置8秒客户端超时，单次结算且清计时器，忽略迟到回包。未扩展create/join/rematch/useItem/syncScore；客户端超时不取消已经到达云端的执行。保存期间显示具体同步状态，失败/超时提示“尚未确认/同步超时，请重试连接”，先query核对后才可继续配置/准备，不自动重放写请求。旧query失败按revision隔离；退出token防止旧房失败提示污染后续会话。
- `render/battle-ui.js`：等待保存/离线时道具加减按钮禁用，重连/退出入口保留；沿用现有布局、圆润排版与颜色。仅本地隔离预览新增`tools/preview-battle-wait.cjs`，SDK/存档/网络均为模拟，不接微信服务。
- 通过证据：新增`test/battle-wait-resilience.js`验证真实Main与平台适配器下头像异常不中断帧循环/引导、无逐帧重试、道具和准备8秒超时、query恢复、迟到回包/失败、跨房退出隔离、脱敏与清timer。`user-profile.js`、`battle-client.js`、`battle-rematch.js`、`cloud-battle-diagnostics.js`、`ui-smoke.js`通过，受影响源码语法通过。首次诊断测试发现null fail契约偏差，已改为显式failed位，失败项及受影响专项重验通过。
- 包体：当前静态主包估算3,995,424 / 4,194,304字节（含其他任务修改，不是微信上传最终包），预算通过。UI检测器对修改的battle-ui返回空问题列表；不代替运行与真机验收。
- 运行证据：CUA实际打开本地Canvas，390×844与320×568模拟尺寸；头像API刻意抛错时引导可见并可关闭，帧计数持续增加且头像尝试始终1次，390道具从1/2调整到2/1；模拟断网后恢复正常并核对回2/1。320实际触发保存不回调，读到“道具同步超时，请重试连接”、同步锁已释放、重连提示/禁用按钮不溢出；点击取消返回menu，释放旧回包仍保持menu。此画面证据在退出token补丁前启动的同一预览产物上取得，只用于未变更的头像/等待页显示与交互；最终退出token另有专项和独立VM证据。
- 非实现者Goodall独立复核：最初指出旧leave失败污染后续会话，已修复；独立fake-cloud VM确认startBattle/joinBattle接管后旧退出不再弹提示，无新增阻断，未重复已有有效套件。
- 下一步由用户在微信开发者工具重新编译，并以当前客户端预览真机验证入房、调整道具、准备、弱网恢复与退出重进；若仍有间歇建房失败，需按新时间关联手机诊断与云日志，不能将条件性头像异常风险当成15:47已证实根因。官方隐私协议失败仍是独立未完成事项。

## 2026-09-22 15:47 入房卡顿排查（只读诊断，未定因）

- 用户报告新上传版本偶尔建房成功，入等待房间明显卡顿且无法调整道具，随后退出；当前上传包与本地并行改造源码是否完全一致尚未确认。
- 原生云控制台最初因MAIN连接超时无法加载；仅关闭并重开该控制台窗口后恢复。实际“全部函数 / 最近1小时”列表显示15:47:02一条battle记录，状态200，执行344ms、内存约24.85MB。展开请求链路确认业务返回`ok:true`及房间编号，冷启动757ms（InitRuntime8ms、InitFunction749ms），重试0次。这是建房成功证据；云端约1.1秒不包含客户端传输耗时，不能用来证明客户端网络正常。
- 当前可见列表未见后续query/configureItems记录，不把日志缺失直接当成客户端未发起请求的证明。模拟器控制台未包含手机此次日志；尚无手机异常堆栈或调用耗时证据。
- 已验证的条件性代码风险：真实Main.loop/render及user-profile模块在本地VM、假wx和绘图桩下对照运行；正常分支安排下一帧且绘制引导，模拟createUserInfoButton同步抛错分支则下一帧数为0、引导未绘制且handleGuideTouch仍吞点击。涉及`js/platform/user-profile.js:ensureButton`、`js/main.js:render/loop/handleGuideTouch`。仅证实异常会卡死画面的路径，不证明15:47手机实际发生头像API异常，也不把隐私配置直接判为根因。
- 道具调整另有交互风险：显示数量依赖configureItems成功返回，actionPending期间后续点击被忽略；无应用层超时（依赖SDK）。慢/挂起请求可能表现为按钮无响应，尚无当次请求证据。
- 下一步：获准后优先隔离可选头像API异常，并为道具同步增加有界等待及明确失败反馈；手机复验时获取脱敏的入房/查询/配置调用阶段和异常类别，分别验证客户端冻结与网络请求失败。此次未改业务代码、上传、部署、修改后台、清缓存或提交推送。以上短脚本验证未接触账号、网络或真实存档。

## 2026-09-22 隐私阅读入口改造（本地完成，未上传）

- 最新现场结论：用户确认点击后出现等待并进入本地协议。开发者工具Console实际读到完整`loaded → settings_touch → button_hit → request → thenable_returned → callback_fail → local_fallback`，本次是官方fail回调而非timeout；按钮链路与本地回退可用，但失败码/原因尚未取得（当前固定阶段日志不保留错误对象）。此前独立复核Goodall已确认P3接口契约与渠道隔离无阻断。
- 后台新证据：刷新已登录的隐私设置页后，截图确认第二项“本小程序处理了用户信息，将如实填写并及时更新用户信息处理情况”绿色选中，第一项未选中；这替代历史“不处理信息”选中状态。但当前仍是“请确认本小程序对用户信息的处理情况/确认以上内容”页面，未读到后续完整协议或生效回执；不能据此断言用户未提交过，也不能宣称官方协议已生效。未点击“确认以上内容”（正式声明需行动时授权）、未修改后台或代码。下一步需获准确认该声明并查看后续配置，或由用户提供后续已生效正文/状态。
- 当前阻塞：用户在超时保护改造后仍反馈“打不开，也没有提示”。尚未确认本次是模拟器还是手机、其他设置按钮是否可用；已询问。不可继续把无回调当成已确诊根因。按钮命中区域与Main分支已检查，未发现可直接证明失效的命中错误；需要实际点击阶段证据。
- P3现场证据：已只读获取开发者工具Console，基础库3.16.2、启动时间13:47:33，真实出现`[privacy-P3] loaded`；证明该工具实例载入诊断版，未见settings_touch/button_hit/request回执。未执行控制台里遗留的命令或上传；下一步用户在该实例实际点击后读取新增P3阶段，不再以本地mock代替现场结果。
- P3诊断准备：直接核对官方[小游戏openPrivacyContract接口](https://developers.weixin.qq.com/minigame/dev/api/open-api/privacy/wx.openPrivacyContract.html)，文档明确不支持Promise风格。已移除返回Promise fulfilled即成功的非契约判断，只有官方success回调确认成功，Promise拒绝仍处理；此前“Promise成功”测试结论由本项替代。新增固定`[privacy-P3]`日志：loaded、settings_touch、button_hit、request、thenable_returned、callback_success/fail、timeout/local_fallback等。仅开发渠道或已识别模拟器输出，release/trial优先禁用；未知真机渠道不输出。不保存正文、身份字段或原始错误，不新增上传/上报。`privacy-entry.js`新增Promise resolve不冒充成功及渠道隔离/日志序列覆盖，与reader/runtime及两处语法检查通过；包体估算3,990,908 / 4,194,304字节（含并行任务工作区）。本次未取得真实微信回执，不宣称官方协议恢复；下一步取重新编译后点击一次的P3控制台记录。
- 最新反馈与修复：用户反馈“点击隐私协议没反应”。代码确认缺少无回调超时，已用新增fake-timer用例复现原代码失败；这证明存在无限等待漏洞，**未取得真实微信回执，不能断言它是本次平台故障的根因**。现点击后按钮显示“正在打开…”，5秒无回调则按既有token保护进入本地协议、释放点击锁；成功/失败/抛错清理timer，迟到回调不能重复结算。没有重试云请求、记录同意或开启后台授权弹窗。
- 最新验证：`privacy-entry.js`（包括无回调、5秒边界、超时后重开、迟到成功/失败）、`privacy-reader.js`（含等待文案/三宽度）、`runtime.js`通过；runtime/main/ui语法和差异检查通过。`tools/preview-level-entry.cjs`新增仅本地`privacyApi=silent`模式，在390宽真实Canvas手动点击看到“正在打开…”，随后实际进入完整本地阅读页（state=privacy），模拟不接触微信账号/存档/云端。Goodall只读独立mock验证成功后迟到timer、超时后迟到Promise/回调均只结算一次，结论无阻断；未重复既有测试。静态包体估算3,987,098 / 4,194,304字节（67文件，包含并行功能改动）；实际微信页面、真机生命周期仍待验。
- 后续开发者工具实测尝试：用户明确确认后台隐私授权弹窗**未开启**。已在正确`match3-wechat`项目的Stable 2.02.2608040工具重新编译，模拟器首页启动，基础库3.16.2；自动点击设置图标/文字均未切页。改用Console直接验证时粘贴超时，AX设置文本与画面仍显示的旧命令不一致，故未按回车执行任何命令。**尚未取得真实`wx.openPrivacyContract`成功/失败回执，不能宣称开发者工具可打开官方协议**。未上传、修改后台、启用授权弹窗或调用云探针；需用户手动点击设置→隐私说明并提供实际页面/错误，或恢复可靠工具交互后再验。
- 用户确认已更新后台隐私保护指引，并授权恢复官方协议阅读入口；新后台正文与审核生效状态尚未逐项核对。本节更新下方历史“本地页/声明待修改”的状态，不意味着正式版建房故障已解决。
- 设置“隐私说明”优先调用 `wx.openPrivacyContract`；API缺失、同步抛错、失败回调或Promise拒绝时进入原有本地完整阅读页。成功不进入本地页；重复点击合并，回调只结算一次，离开设置后的旧回调不会劫持页面或清除新请求。未写入用户同意状态，未改变云调用、鉴权、协议正文或平台配置。
- 涉及文件：`match3-wechat/js/core/runtime.js`、`js/main.js`、`test/privacy-entry.js`、`test/privacy-reader.js`。已通过两处JS语法检查以及 `privacy-entry.js`、`privacy-reader.js`、`runtime.js`、`package-budget.js`；当时工作区静态主包估算3,890,637 / 4,194,304字节（含其他任务变更，不代表最终上传包）。后续并行功能改动须按影响范围验证，勿将此估算视为新产物证据。
- 运行检查：现有本地Canvas预览390宽下，从设置点击隐私说明实际进入完整本地阅读页并可返回设置；无微信API环境，不能替代真机官方页面验证。已有阅读测试覆盖320/390/430宽布局及翻页。
- 独立复核发现并修复：协议请求挂起时接受好友邀请，入房失败后迟到的协议失败回调可能切走页面。现于`joinBattle`接管交互时作废旧协议请求；专项新增真实Main入房的无效邀请/业务失败/网络拒绝三分支回归，迟到回调不再切页，后续仍能重新打开协议。修复后`privacy-entry.js`、`battle-client.js`及Main语法检查通过；因源码变化重验包体预算通过，当前静态估算3,896,992 / 4,194,304字节（含并行任务变更）。
- 非实现者Goodall独立复核确认上述竞态闭环、无本次改造阻断；没有重复已通过的测试，未把mock验证当成真机证据。最终差异空白检查通过，临时本地预览服务已停止。未产生需另存的非重复长期记忆候选。
- 官方[小游戏隐私合规开发指南](https://developers.weixin.qq.com/minigame/introduction/guide/privacy.html)本次直接核对：`openPrivacyContract`仅展示指引；自定义授权用`onNeedPrivacyAuthorization`上报曝光/同意/拒绝，或在后台开启官方隐私弹窗组件。标准个人信息API可触发授权，非标准信息处理需自行在处理前适配；头像接口`createUserInfoButton`在其标准API清单内，`wx.cloud.callFunction`不在该页清单中，不能仅凭本次文档将-9003归因为隐私授权失败。官方组件说明“一经启用无法关闭”，本轮未代为启用。
- 发布前待验：确认后台指引已审核生效、正文与本地说明/实际数据处理一致；在微信真机点击设置入口，确认官方完整协议可读并正常返回；确认授权弹窗模式及拒绝后的可选头像行为。若官方弹窗尚未开启且未实现自定义授权，仍需独立补齐授权方案，不可把此阅读入口改造当成全部隐私合规完成。本轮不上传、部署、提交或推送。

## 2026-09-20 正式版建房失败排查（未定因）

### 当前结论（2026-09-22）：完整依赖部署后启动恢复，正式版 -9003 仍未解决

- 隐私分支只读核查：用户提出隐私声明疑点后，后台“账号设置→基本设置”显示“用户隐私保护指引：已更新”，弹窗状态“可设置”。进入“去完善”后的小游戏隐私页面截图仍显示第一项“不以任何方式处理用户的任何信息”为选中样式，两项文字/控件均灰显；未点击选择或“确认以上内容”，不宣称可以直接修改或确认已发布协议正文。本地协议已明确披露云端OpenID、对战记录及可选头像，实际建房直接cloudBattle.call('create')，没有getPrivacySetting/requirePrivacyAuthorize/onNeedPrivacyAuthorization授权流程。故至少存在后台可见声明与功能的信息处理口径不一致，需要优先核实/纠正；**尚无证据证明该不一致导致-9003，也未确认wx.cloud.callFunction在本小游戏版本受哪项隐私授权门槛控制**。本地长文说明不等于微信平台声明/授权，不盲加授权API或通过勾选修复猜测。隐私页面保留，后续声明提交与代码授权改造需用户批准并核对适用官方规则。
- 用户补充确认：当前重新生成的手机预览版已可正常使用好友对战，正式版仍失败；这是修复部署后的新渠道对照。随后授权只读核对后台版本、环境状态和访问规则，不修改配置、套餐、权限或发布。
- 微信公众平台已由用户扫码登录，本次实际读到：猫猫开心消线上版本**1.0.1**，发布时间**2026-09-21 22:33:42**，备注“增加好友对战的诊断埋点”；开发版本1.0.1代码提交时间**2026-09-20 23:44:55**，暂无待审版本。版本详情没有展示部署源码/云环境常量，不能仅凭版本号或备注宣称正式包与当前预览包完全一致。后台AppID与项目一致；未读取/生成密钥或上传凭证。
- 本地聚焦检查：云环境常量与已提交代码一致，当前未提交改动主要为D1/D2诊断包装；初始化/调用路径未按release切换环境，唯一wx.cloud.init仍指定cloud1-d9g4pv8m8457af92a。此为本地源代码证据，不是已发布包反编译验证。
- 实际云函数权限截图：所有函数`invoke: auth != null && auth.loginType != 'ANONYMOUS'`，未见按开发/正式渠道分流规则；环境级“未登录用户访问权限”关闭。页面说明控制台/服务端不受此客户端规则约束，因此控制台探针不能证明正式版权限链路通过；当前不放开匿名权限。
- 环境列表确认属于猫猫开心消，ID与客户端一致，仍为“免费开发环境”。套餐管理显示现有配额，按量付费与自动续费未开启；套餐页/环境管理本次未见停服、欠费、到期或明确禁止正式版调用的提示，也未显示可确认的有效截止日期。没有证据支持付费升级必然修复。
- 微信公众平台“开发管理→开发设置”实际显示服务器域名尚未配置，同时明确说明“使用微信云开发/微信云托管无需配置服务器域名”“无需服务器及域名配置即可上线小程序”。当前建房使用原生wx.cloud.callFunction，不能将空域名列表直接判为原因，不擅自添加SDK域名。代码上传IP白名单属于上传安全，不当作手机云调用白名单。
- 本轮没有找到可确诊的后台配置错误；未修改应用/云配置、权限、套餐、代码、凭据、版本，未再做上传或建房。后续应获取正式版的完整平台错误/关联请求线索，或携00:42失败时间、当前部署时间及同手机预览成功对照向官方请求平台侧定位；尚未提交咨询/工单。正式版本管理标签保留供用户核对。

### 本轮部署与运行证据

- 用户确认采用“上传并部署：所有文件”且显示成功，随后00:42正式版仍报`D1/CALL/-9003/NETWORK`、无初始化行。只读控制台确认battle已部署，更新时间**00:40:27**；随后00:40:36–43共8条执行状态200，耗时151–300ms、约22–24MB。这证明新部署可执行，不单凭200证明建房业务成功。
- 重新进入“全部函数 / 最近1小时”日志页，最新记录仍为00:40:43，未见00:42对应调用；暂无请求ID关联证据，不能仅凭日志缺失认定正式版绝对未到云端或判定套餐/域名原因。
- 对新部署执行一次此前获准的无房间读写探针`{"action":"diagnostic_readonly"}`，截图确认真实返回`{"ok":false,"err":"身份校验失败"}`，执行4ms、运行内存23.72MB，冷启动642ms（InitRuntime10ms、InitFunction632ms）。控制台测试不带玩家OpenID，此返回符合当前入口保护，证明已进入业务代码；**不是正式版用户鉴权失败的证据**。此前的443/0ms进程退出本次未复现。
- 本轮不再部署、不改配置/权限/套餐/源码、不清缓存、不创建房间。下一步应在当前部署下补充同一手机的最新预览/正式渠道对照；旧预览成功发生在更早部署、最近一次预览失败又叠加443，均不足以替代此对照。若预览恢复但正式仍失败，再聚焦正式客户端/微信调用入口，不能继续用重传依赖试错。

### 历史：启动失败与依赖重建

- 用户提供9月21日23:54手机截图：`D2/CALL/-1/UNKNOWN`，开发版，错误链`-1,-504002`，无初始化行。D2保留了内层数字码，但词汇白名单过滤过严，隐藏了有价值的自然语言；不再靠此残缺文字推断域名、套餐或权限原因，也未继续改客户端/生成额外预览。
- 实际控制台“全部函数 / 最近1小时”读到23:53:43、23:15:13、23:11:40、23:11:33四条`battle`失败，均状态443、运行0ms、内存使用0。展开23:53:43请求链路得到 `errorCode:-1`、`errorMessage:"0 code exit unexpected"`、`statusCode:443`；Coldstart8ms（InitRuntime8ms、InitFunction0ms）。这是运行环境/函数启动失败的具体证据，不是HTTP443端口或域名白名单提示。
- 新查部署更新时间为 **2026-09-21 23:02:57**，不是前次检查的9月16日；用户确认自己重新上传，选择“云端安装依赖”。运行环境仍为Node.js20.19，入口`index.main`，流量100%至`$LATEST`。没有改配置。23:02之前的开发/预览成功证据不能证明新部署正常；初始正式版`-9003`是否与当前`-504002/443`同因尚未证实。
- 官方[云函数状态码](https://cloud.tencent.com/document/product/583/42611)说明443为`UserCodeError`，建议查错误堆栈；并非身份鉴权401、欠费410或超时433。当前日志没有进一步的模块堆栈，不能把具体原因定为缺少某个依赖、运行时版本缺陷或免费套餐限制。
- 在控制台仅执行一次无房间读写入参 `{"action":"diagnostic_readonly"}`，独立于手机客户端。面板标题“测试结果：成功”，但真实返回 `errorCode:1`、`errorMessage:"0 code exit unexpected"`、`statusCode:443`，无运行时间/内存摘要，判定失败而非通过。没有创建房间或部署函数。
- 本地Node v24.19.0实际加载`cloudfunctions/battle/index.js`成功、导出main，未调用业务；npm ls显示依赖树完整（wx-server-sdk4.0.2、@cloudbase/node-sdk3.17.2及项目锁定覆盖）。本地源码无主动process.exit/abort；本地成功不代替云端Node20验证，也不证明云端安装结果与本地相同。
- 用户已明确授权固定锁文件重建依赖并仅重新部署`battle`，随后执行无房间读写云端探针；保持数据库、权限、套餐、环境与运行时不变，不上传客户端、不提交/推送Git、不发工单。初始正式版故障仍须单独验证。
- 重建产物：`/private/tmp/match3-battle-redeploy-uC8xwm`，仅复制现有battle源码/manifest/lock/.npmrc/vendor；首次沙箱下载ENOTFOUND，获准联网后`npm ci --ignore-scripts --no-audit --no-fund`安装104包成功，锁文件不变。Node24.19.0/npm11.17.0下`npm ls --all`、`cloud-dependency-security.js <artifact>`、入口require/main导出检查通过，`npm audit --omit=dev`为0项已知漏洞。未找到本机Node20；云端Node20启动验证尚待部署。
- 原`node_modules`已可恢复地移到上述临时目录的`previous-node_modules`，将干净重建的node_modules复制回battle，目录递归比较一致；源码/锁/vendor无差异。开发者工具已确认“上传并部署：所有文件”入口（包含本地依赖），不使用未验证的“云端安装依赖”。部署结果待记录。
- 非实现者Goodall独立只读复核：产物入口/锁/.npmrc/vendor与仓库一致，无外部符号链接或平台二进制；仅有内部.bin链接，现有许可证未因复制丢失，自有兼容层与锁定依赖形态正确。未重复依赖安全/审计检查；结论无本地技术部署阻断，不代表云端通过。
- 已在正确项目/cloud1的battle右键选择“上传并部署：所有文件”，实际看到“上传云函数 battle 0%”，随后进度提示消失。**尚未取得云端更新时间或部署成功回执，不宣称部署成功或线上恢复**。后续原生工具出现noWindowsAvailable、windowNotFoundAtPosition及菜单无法收起；通过“居中显示所有窗口”及项目列表重开后仍有坐标定位故障。没有再次上传、清缓存、修改云配置或执行业务测试。下一步需用户手动打开“云开发→云函数”窗口，再核对battle更新时间并执行同一无写入探针；原正式版问题仍待真机复测。

### 历史操作：开发版 D2 临时预览诊断

- 真机调试因工具自身超时不可用后，用户批准改为开发版弹窗显示脱敏错误说明和内层数字码，并生成一次普通预览包；不提审、不发布、不调整云端权限、不提交Git。
- `js/net/cloud-battle.js` 仅在渠道明确为 `develop` 时收集D2详情，展示时再次核对当前渠道；release/trial/unknown维持D1简略提示。只读取errCode/code/errno、errMsg/message以及cause/error/details的最多2层、4个错误对象，去重最多6个数字码；文字仅保留技术词汇，地址、数字及未知文本隐藏，并优先移除敏感标识键值。说明最长320字符，无原始对象存储、日志或远程上报，云调用与重试行为不变。
- 新增专项覆盖开发版与其他渠道隔离、展示时渠道变更、嵌套码、中文域名提示、敏感标识/URL/手机号隐藏、循环引用、深度与长度限制。专项首轮测试样例含network敏感值导致原有分类匹配NETWORK，修正样例后通过；`battle-client.js` 与包体预算通过，静态主包估算3,874,623 / 4,194,304字节（含当前其他任务工作区变更，不单独归因本次）。JS语法与差异检查通过。
- 合成错误的内存mock输出确认D2能保留 `request:fail url not in domain list`、`TLS handshake failed` 和内层-9003，同时隐藏测试URL；这不是实际平台错误或根因证据。原worker Goodall（固定gpt-5.6-luna/max）独立只读复核无阻断；指出人为改变渠道后历史对象仍显示生成时渠道标签，但详情已隐藏，真实运行渠道不动态变化，非阻断。复核未修改文件、未全库检查或全套重测。
- 复核完成后已生成普通预览二维码：界面显示代码包3633KB、9月22日00:15失效；构建区显示重新编译 `js/net/cloud-battle.js`、上传完成，截图确认二维码实际显示。当前只上传至微信预览服务，未提审/发布；待用户扫码并回传手机D2失败提示。配置保持域名校验开启，未恢复为绕过校验。

### 历史操作：获准生成一次真机调试包

- 用户明确批准将当前工作区上传至微信真机调试服务，用于读取失败的原始错误；不提审、不发布、不调整云权限。已点击“真机调试”，工具显示代码包3630KB，默认Android/鸿蒙模式，当前停在“等待进程”，尚未确认二维码生成或手机连接。
- 已询问手机系统以选择正确的调试模式。拟在 `js/net/cloud-battle.js:106` 的失败回调设置运行时断点，必要时查看同步异常分支110行，只读取错误说明及必要的脱敏线索，不保存完整错误对象或身份字段；尚未设置断点或得到原始错误。
- 两处 `urlCheck` 仍为 `true`。未为此次调试修改业务源码、部署云函数或创建测试房间。后续需确认工具启动结果及手机连接，不能把点击调试入口等同于调试成功。
- 用户确认手机为iPhone。已选择iOS单选项（AX值1），但工具持续停在“等待进程”；尝试取消卡住的启动并重开调试面板后，出现 `真机调试 Error: Timeout [2.02.2608040][darwin-arm64]`。构建区截图显示打包和上传完成，之后初始化调试环境/实例并等待进程；未生成可用二维码、未连接手机或取得原始云错误。该超时属于调试工具启动失败，不是正式版云调用错误的定因。需要先重启开发者工具恢复调试进程；不继续重复上传试错、不清缓存或杀进程。

### 2026-09-21 本地域名校验对照准备

- 用户删除小游戏重新进入后仍建房失败；该操作未解决故障，不继续要求清存档。
- 用户明确批准开启本地合法域名校验。仅将 `match3-wechat/project.config.json` 和 `project.private.config.json` 的 `setting.urlCheck` 从 `false` 改为 `true`，避免私有配置覆盖；没有改业务代码或后台域名列表。
- 两文件 JSON 解析与布尔值断言通过；开发者工具执行编译后，截图确认首页启动、控制台新启动时间23:09:25，显示1条HarmonyOS兼容提示，未见启动错误。编译后再次读取两文件，`urlCheck` 均仍为 `true`。这只证明配置落盘和本地启动，不证明云调用通过或正式版故障已解决。
- 本轮未主动调用真实建房、未上传预览/体验/正式包，未提交Git、修改云权限、购买套餐或清理存档；保留其他任务的未提交修改。
- 下一步由用户重新生成预览并扫码测试好友对战（不要复用旧预览二维码），回传是否成功及完整域名/调用错误。若需要助手代生成预览，须另行明确允许向微信上传当前工作区预览包；当前批准范围仅本地配置。
- 后续用户反馈失败、错误码变为 `-1`，尚缺完整诊断串（阶段/分类/渠道/初始化）和原始平台错误说明。工具界面确认用户已生成预览二维码，两处配置仍为 `true`；本地 Console 仅见启动信息和HarmonyOS提示，没有手机失败的详细错误。不能把普通扫码预览的手机日志等同于模拟器日志，也不能认定 `-1` 就是域名白名单失败或与正式版 `-9003` 同因。
- 当前下一步：先取得完整失败弹窗；若仍无具体原因，需要在真机调试中捕获原始调用失败说明并仅保留脱敏后的错误短语/必要目标主机，不再仅凭顶层数字码猜测。尚未改诊断实现、发起真机调试上传或变更白名单。
- 用户进一步确认完整提示为 `D1/CALL/-1/UNKNOWN`、开发版、无初始化行。开发版与扫码预览相符；`UNKNOWN` 为本地分类未匹配，不能解释为官方未知错误，亦不能断言域名故障。尝试在开发者工具输入无写入 `diagnostic_readonly` 探针时，粘贴超时；未按回车执行，未取得调用证据。下一步需获准生成微信真机调试包，并在手机失败回调中查看原始错误（不以调试成功替代正式版修复验证）。

### 最新用户反馈：诊断版本返回 `D1/CALL/-9003/NETWORK`

- 用户确认弹窗渠道为正式版，没有初始化异常行；2026-09-21 22:43（北京时间）同一手机分别使用 Wi-Fi 和移动数据建房，均返回上述诊断码。没有初始化行只表示未捕获到同步初始化异常，不证明异步云服务/身份初始化完全正常。
- 根据当前 `cloud-battle.js`，`CALL` 表示 `wx.cloud.callFunction` 的失败回调或同步异常，不是服务端业务失败分支；不能仅据此证明函数完全未执行。`-9003` 为提取的数字错误码，`NETWORK` 是原始错误文字匹配 `network|网络|offline` 后生成的本地分类，并非微信官方定因。
- 本次网页检索没有获得适用于该原生云调用场景的权威 `-9003` 定义，不能套用其他腾讯产品的同号错误，也不能据此认定免费套餐、域名或手机网络为根因。
- 22:43 之后实际打开控制台“全部函数 / 最近1小时”查询，并在核查配置后再次查询：两次仅见 2026-09-21 22:37:24 的 `battle` 调用，运行状态200，耗时374ms，内存约24.56MB（配置256MB）。未见22:43记录，支持调用进入函数前失败的方向，但不排除日志遗漏/延迟等情况；该成功调用的来源和业务结果未知。
- 当晚重新核实：`battle` 已部署（更新时间2026-09-16 00:00:35），环境ID一致，套餐为“免费开发环境”；套餐页未见明确到期/停服提示，不能据此证明正式版可用或不可用。云函数调用规则仍为 `auth != null && auth.loginType != 'ANONYMOUS'`，无显式渠道条件；环境“未登录用户访问”关闭，未为试错启用。以上规则不证明失败请求实际获得了合格身份。
- 下一步建议由微信云开发官方根据 AppID、环境ID、函数名、2026-09-21 22:43（UTC+8）及错误码查询平台调用/接入链路；对外提交需用户明确授权，尚未发送。原始 `errMsg` 在当前版本被有意脱敏丢弃，仅此诊断码无法还原更细原因；不承诺无需补充证据即可确诊。
- 本轮只读核实后台并更新记录，未改客户端、云配置、权限、套餐或发布状态，未执行测试建房，也未提交咨询。

### 2026-09-21 八项核验（只读）

1. 环境 ID：小游戏入口是 `game.js` 而非 `app.js`；`Main` 调用 `cloudBattle.init()`，显式指向 `cloud1-d9g4pv8m8457af92a`，与前次控制台环境一致。没有按开发/正式版自动切换环境的代码；实际线上包是否为同一源码仍未核实。
2. 云函数部署：复用前次控制台证据，`battle` 已部署、更新时间 2026-09-16 00:00:35；本地云函数最近提交为 `b7e4c76`（2026-09-15 23:27:14 +08:00），此后无本地云函数改动。时间先后不证明云端文件/依赖与本地完全一致，开发者工具及手机预览真实建房成功可证明对应渠道主链路可运行。
3. 日志：复用 23:08 正式版重试后的日志查询，无对应执行记录。9月21日本次概览看到近期云函数/数据库调用及“当前暂无运行异常”，错误日志图为0；不等于正式版没有调用前错误。本次详情导航无响应，未取得新的正式版原始错误或更细日志。
4. 数据库权限：建房由 `wx-server-sdk` 在云函数内读写 `battle_rooms`，客户端没有直连集合。前次用户已确认仅服务端访问；本次未重新读出实际集合规则。仅服务端访问是既定安全边界，不应为此故障改成所有用户可读写。开发/手机预览建房成功也证明这些调用的服务端读写可用。
5. `config.json`：本地 `permissions.openapi=[]`，当前云函数不调用 `cloud.openapi`，不缺少业务所需的 OpenAPI 声明。客户端调用云函数的安全规则是另一项配置，前次查看为非匿名已认证用户可调用；定时清理声明仍为 `0 0 3 * * * *`。本次无须为此重新部署。
6. 合法域名：`project.config.json` 与 `project.private.config.json` 均为 `urlCheck:false`，这是明确的开发/发布校验差异，不是已证实根因。建房链路使用 `wx.cloud.callFunction`，无普通 HTTP/WebSocket 或第三方请求；可选头像通过 `wx.createImage` 加载动态 HTTPS 地址且失败不阻止建房。后台域名清单本次未验证，不能宣称完整；不盲加 SDK 域名或放开权限。
7. 超时/内存：复用前次配置 3秒/256MB；既有调用样本约132–412ms、约23–25MB，未显示接近限额，但这些是此前成功样本，不代表正式版失败请求或冷启动。暂无证据支持修改超时/内存。
8. 缓存/真机：用户已确认同一手机扫码预览建房成功；这不是后台设定的“体验版”验收，也不是正式版验收。本次不清除开发者工具或手机存储，避免删除关卡、金币等存档。若后续验证缓存，仅考虑编译/文件缓存且须明确授权。

结论：未发现可确诊的建房源码配置错误；发现本地关闭域名校验这一差异，仍需更严格预览验证或正式版诊断码。未修改代码、设置、套餐、数据库权限、缓存或线上服务；未重复运行此前仍有效的通过测试。

### 诊断改动交付记录（历史；最新线上反馈见上）

- 用户授权本地增加建房失败诊断；不提交、不推送、不上传微信，不变更云配置或套餐。
- `js/net/cloud-battle.js` 提取数字错误码与固定分类，记录内存中的脱敏初始化异常；仍尝试原有调用，不增加自动重试。返回渠道仅允许开发版/体验版/正式版/未知，不展示 AppID、OpenID、请求 ID、原始消息或堆栈；无新增存储、日志或远程上报。
- `js/main.js` 建房失败时用原生确认弹窗展示 `D1/SDK/...`、`D1/CALL/...` 或 `D1/SERVER/...`；若初始化曾失败另列初始化线索。SDK 表示云能力不可用，CALL 表示调用失败，SERVER 表示已收到不成功/缺字段的响应。分类仅是排查线索，不等于确诊。限流仍用原提示，成功流程保持不变；弹窗不可用则降级为提示。
- 自动验证：Node 语法检查两处客户端源码；`node match3-wechat/test/cloud-battle-diagnostics.js`、`node match3-wechat/test/battle-client.js`、`node match3-wechat/test/package-budget.js` 均通过。覆盖敏感字段不输出、初始化失败与重新初始化成功、同步抛错、SDK 缺失、异常码格式、业务失败、限流、取消后迟到失败及失败后重试成功。
- 独立复核：worker `01a0bf6d-1376-7f92-aa59-427672636ffa`（工具固定 `gpt-5.6-luna / max`）只读审查无阻断；另用内存 mock 验证“取消后立即重试，旧请求携敏感字段迟到失败不弹窗、不污染新请求，新请求失败只显示脱敏编号”。未改文件、未重复已有通过检查，不代表真机验收。
- 当前静态主包估算 `3,863,629 / 4,194,304 bytes`（59 文件），较改动前增加 4,005 bytes。
- 开发者工具原生弹窗尝试未完成：控制台自动输入丢字符导致样例语法错误，改用粘贴仍超时；未调用真实云服务。自动测试不代替原生弹窗/手机验收。
- 用户后续确认手机预览版建房成功；结合开发者工具建房成功，当前环境的函数、身份和数据库建房链路在开发/预览渠道可用。正式版失败仍未取得错误码，不能以预览成功宣称线上故障已修复，也不能据此确认免费套餐是原因。
- 下一步需核实实际正式版代码身份及其错误码。若将本地诊断带入正式版，须用户另行批准上传/提审/发布；当前仅本地改动授权，不自动执行 Git 或微信发布动作。诊断弹窗的真实失败显示仍待验证。

### 已有排查证据

- 网页核对（2026-09-20）：CloudBase [创建环境](https://docs.cloudbase.net/quick-start/create-env) 的云控制台创建章节概述“开发免费、正式使用需付费”。[配额制价格文档](https://cloud.tencent.com/document/product/876/75213)（页显更新 2026-08-31）同时明确微信 IDE 免费期环境属历史套餐，且描述发布后到期时间改为上线后第15天；[资源点价格文档](https://cloud.tencent.com/document/product/876/127357) 也写有发布后第15天到期、之后停服隔离的规则。不能由此推导“所有免费环境正式版发布当日一律禁用”；本项目历史套餐的实际适用条件仍需核实。资源点模式切换后不可切回原计费模式，不为试错擅自切换。
- 社区搜索发现“体验版本测试OK，发布版本异常”的合法域名排查案例，但[原帖](https://developers.weixin.qq.com/community/develop/doc/000e847c78460018a2f26c84f61000) 因浏览器安全限制无法读取，只有搜索摘要，不作为已验证解决方案。项目建房使用原生 `wx.cloud.callFunction`，不能直接套用普通 `wx.request` / uniCloud 案例的域名或公开数据库权限建议。
- 不必只等待诊断版审核：可以并行核对后台实际环境有效期/停服提示/发布访问条件，并通过平台官方支持确认该历史套餐是否存在渠道限制；本轮未代发咨询、改套餐或提交任何材料。正式版错误码仍是确诊关键证据。
- 用户确认首版已上线，正式版点击好友对战提示“暂时无法创建对局，请稍后重试”。以下历史测试通过不代表本次线上故障已解决。
- 只读检查：客户端仍指定 `cloud1-d9g4pv8m8457af92a`；控制台对应环境套餐仍为“免费开发环境”。`battle` 已部署，最近更新时间为 2026-09-16 00:00:35，运行时 Node.js 20.19，超时 3 秒。
- 控制台最近 24 小时日志显示：最新一条为 2026-09-20 03:00:15、运行状态码 200；此前 2026-09-19 23:41/23:32 有状态码 200 的调用。200 仅说明函数调用完成，不能证明业务返回成功；未获得与用户失败操作时间对应的请求证据。
- 云函数调用安全规则为 `auth != null && auth.loginType != 'ANONYMOUS'`，未见按开发版/正式版分别限制的规则；本次未修改权限。
- 客户端 `startBattle()` 将调用异常及多数业务失败统一显示同一句提示，并丢弃 catch 的原始错误。仅凭该提示不能区分云环境/网络/身份/服务端故障。
- 用户在 23:08 用手机正式版重试仍失败；约 23:09 控制台重新查询“全部函数 / 最近 1 小时”显示暂无数据。暂未看到对应函数执行记录，倾向于函数执行之前的失败，但日志延迟、客户端实际配置等尚未排除，不能单凭日志缺失认定平台拦截。
- 尝试开发者工具 Console 无写入连通性探针（未知 action 只返回“未知操作”），未成功执行：原生控制台输入未生效，随后两次界面操作均报窗口不可访问。没有取得探针响应或原始错误码。
- 随后用户手动执行相同探针，返回 `{ok:false, err:"未知操作"}`，符合预期；用户进一步确认开发者工具点击好友对战可以进入房间。开发端函数调用、身份与实际建房链路可用，排查重点转为正式版客户端/平台调用差异。尚未取得正式版原始错误码或核实实际线上包与本地源码一致，不能据此证明所有服务端路径均无故障。
- 初始只读排查阶段未改代码或创建房间；后续仅加入上述获授权的本地诊断。免费环境的生产可用性仍是待验证疑点，不能据此宣称付费升级必然修复。未部署、购买或调整云配置。

## 验收范围

- 测试环境：`cloud1-d9g4pv8m8457af92a`
- 云函数：`battle`
- 云数据库集合：`battle_rooms`
- 使用两个真实微信账号、两台真机；每次测试记录房间号。
- 本轮只验收测试云环境，**不提交审核、不发布生产版本**。

## 测试记录

2026-09-15补充：最终检查修复了客户端会话回调/弱网分数重试/截止输入与云函数等待退出席位。须重新上传`battle`云函数（依赖版本不变），再上传同一客户端体验版本；旧云函数不能验证新席位行为。复测重点：等待中一人退出后可加入新好友并重新准备、对局中短暂断网恢复后的分数同步、60秒之后停止操作、退出重进不被旧回调切回。长断网或截止后抵达的分数仍不补录，按服务端结算。

- 日期/版本：
- 账号 A（房主）/设备：
- 账号 B（受邀者）/设备：
- 开发者工具环境选择：

## 2026-08-23 已确认状态

- 产品负责人确认 `battle` 云函数已上传，`createdAt` 索引已生效，客户端直连 `battle_rooms` 被拒绝。
- 产品负责人确认双账号真机核心链路正常；弱网、前后台切换、主动退出和邀请过期仍按下方清单单独验收。
- 产品负责人确认 `cleanup-battle-rooms` 已按控制台时区每日 03:00 实际触发，并能清理 7 天前的测试房间且保留新房间。
- 当前免费开发环境未提供所需告警监控能力；云函数错误、调用量和费用告警延后到升级付费或正式环境时配置。
- 当前不上传微信客户端包、不提交审核、不发布正式版本。

## 验收步骤

- [x] 1. 环境准备：确认开发者工具选择 `cloud1-d9g4pv8m8457af92a`，`battle` 云函数已部署且可读写 `battle_rooms`；验证客户端直连 `battle_rooms` 被拒绝；确认 `createdAt` 索引和 `cleanup-battle-rooms` 每日定时触发器已生效；两台设备使用同一测试版本。
- [ ] 2. 创建邀请：账号 A 进入游戏，点击好友对战并成功创建房间；记录房间号，发出带房间参数的微信邀请。
- [ ] 3. 邀请进入：账号 B 从邀请卡片进入，成功调用 `join` 并进入同一等待房间；双方显示的对手身份正确。
- [ ] 4. 双方准备：A、B 分别点击准备，双方状态均同步为已准备；单次点击不重复扣除或改变其他状态。
- [ ] 5. 三秒倒计时：双方准备完成后服务端进入 `playing`，显示约 3 秒倒计时；倒计时结束前棋盘不可操作且不应上报有效比分。
- [ ] 6. 六十秒对局：倒计时结束后双方均可操作；以服务端 `startTime` 计时，约 60 秒到达后停止接受比分和道具请求。
- [ ] 7. 分数同步：A、B 各完成至少一次有效消除，双方看到对手分数更新；分数不倒退、不串号，异常增长请求被拒绝或不改变存档。
- [ ] 8. `freeze` 道具：一方仅使用一次 `freeze`；对手输入被短暂锁定，自己的道具数量只减少一次，效果结束后可继续操作。
- [ ] 9. `disturb` 道具：另一方仅使用一次 `disturb`；对手短暂需要 4 连消，效果结束后恢复普通规则，道具数量只减少一次。
- [ ] 10. 前后台切换：对局中将任一账号退后台并在短时间内回到前台；回到前台后仍能查询同一房间，比分、道具和倒计时无重复或回退。
- [ ] 11. 短暂弱网：对局中制造短暂网络抖动或断网后恢复；恢复后轮询继续，未产生重复道具、重复结算或错误加入其他房间。
- [ ] 12. 主动退出：另开一局，在对局中由一方主动退出；另一方最终收到对手离开并获胜的结算，房间状态变为 `finished`，退出方不能继续提交比分或道具。
- [ ] 13. 正常结算：重新创建一局并等待 60 秒正常结束；双方收到相互一致、视角正确的胜负/平局结果、比分和金币奖励，奖励只发放一次。
- [ ] 14. 重复查询幂等：对已结束房间重复触发 `query`（含重进或恢复轮询）至少 3 次；每次返回相同结算结果，状态不回退，金币、效果和玩家结果不重复生成。
- [ ] 15. 邀请过期：在测试环境构造或保留一个等待超过 10 分钟的房间；受邀者不能加入，房主轮询收到失效后显示明确提示并回到首页。
- [x] 16. 留存清理：在控制台放入 `createdAt` 早于 7 天的专用测试房间，等待定时器执行后确认该记录被删除，新房间不受影响，并在云函数日志核对删除数。
- [ ] 17. 建房限流：同一账号在同一分钟连续建房 6 次，第 6 次显示“创建太频繁，请稍后再试”；跨分钟后可恢复。测试完确认限流控制文档不含明文 OpenID，客户端也拿不到摘要或内部计数。
- [ ] 18. 失败证据记录：任一步失败时记录时间、版本、环境、房间号、账号/设备、操作步骤、截图或录屏、客户端日志及云函数返回/错误信息；不得以“日志打印失败但进程退出码为 0”作为通过依据。

## 发布边界

本清单完成后仍只保留测试云环境结果；本轮不提交审核、不发布生产版本、不执行生产部署。

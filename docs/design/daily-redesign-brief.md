# 每日挑战页面重设计 · 创作前简报

状态：用户于2026-09-22授权内置image工具；已生成1张月光邀请函参考，用户已接受并授权实施；每日详情原生实现与本地运行QA完成。用户已接受首页，首页冻结。

当前规则修订：每天一局，达标奖励500金币，中断可继续同一局；已完成或失败后当天不重开。下文300金币与重复挑战仅记录前序设计和旧版验收，不代表当前规则。

## 目标与改变

前两版分别为大猫居中单面板、深蓝目标区加账目面板；用户均未接受整体布局。保留了艺术字、无当前日期、装饰和300金币规则，但这些局部修饰没有解决页面像数据面板、画面中心缺少活动感的问题。技术QA通过不等于用户视觉接受。

这次使用OpenAI内置image_gen产出一张完整的「月光挑战邀请函」参考，再由用户确认构图；不继续在当前深蓝大数字卡片上调整边距。不能把生成的整页图直接当交互页面。

推荐构图：
- 顶部返回按钮与“每日猫咪挑战”艺术字，保留安全区，不展示当前日期。
- 中间为挂在月光花园里的轻盈活动邀请函，五色琉璃猫围绕挑战徽章，猫咪与星月装饰成为视觉中心。使用现有花园世界，不新增IP或异风格。
- 邀请函直接说清“25步内达到2000分”；当前500金币为有价值感的金币小堆和奖励绶带，个人最佳以简洁的下方记录呈现。
- 下方一个粉金主按钮“开始挑战”，按钮文字与点击区域居中；状态提示放在按钮上方，底部留白按安全区+40逻辑像素规划。
- 所有规则、目标、奖励、纪录、状态、按钮和返回都在Canvas原生实现；装饰可独立提取，不需要新增字体包。

## 必须覆盖的状态

未挑战、加载中、内容/服务不可用、网络失败、开始中、待确认成绩、达标已领取、失败后当天锁定、中断续局、跨日旧局成绩。不能因为连接失败展示可开局或已到账；不能用本地时间和客户端分数发放正式金币。

默认尺寸375×812，另检查320×568与430×932；44px最小触控；低配保留静态画面并关闭非必要特效。画面必须明确属于已确认首页的月光水晶世界。不得使用当前大块深蓝卡片+账目表、整屏大数字仪表盘、过大单只猫或小字挤边方案。

## 已选生产方式与产物

用户明确选择内置image工具，本次已调用1次，仅用项目自有首页截图延续风格。没有启用Lovart/Recraft，没有购买、充值、订阅或上传第三方游戏素材；此前外部供应商比较不再作为待决门槛。工具未返回模型版本或实际费用，不推断为免费。

- 参考图：`assets/_incoming/daily-reference/moonlight-invitation-v1.png`。
- 提示词与来源：同目录`prompt-v1.txt`、`README.md`。
- 实看：浅色邀请函、双猫与琉璃小猫、目标与300金币奖励、单一居中按钮、底部留白；中文主要文案正确。
- 用户已接受参考构图；整张概念图未进包，仅无文字装饰派生进包。实际页面待用户确认。
- 已实施：原生Canvas标题/目标/奖励/记录/状态/按钮 + 无文字邀请函；320/375/430五状态实看、ui-smoke与独立QA通过。375真实Main实际点击重试/开始、25步跨日结算300、返回通过。微信实际接口仍待核查；每日独立结果页保持详情运行版接受后的顺序。

## 后续已批准调整 · 弹窗

用户明确要求删标题、只保留中部心形视觉，将按钮合入背景。当前实现以已接受双猫/月徽邀请函为居中弹窗，首页在后方压暗；全部规则、奖励、纪录、状态和冰蓝主按钮在邀请函内，右上44px关闭X。没有新生成素材、没有重设计首页。加载/开始时禁用主操作，关闭沿用未保存成绩保护，遮罩不触发背后首页入口。有效检查见当前任务记录。
